import "./chunk-PZ5AY32C.js";

// src/repl_bindings.ts
function defineReplBindings(app, repl) {
  repl.addMethod(
    "loadRedis",
    async () => {
      repl.server.context.redis = await app.container.make("redis");
      repl.notify(
        `Loaded "redis" service. You can access it using the "${repl.colors.underline(
          "redis"
        )}" variable`
      );
    },
    { description: 'Load "redis" service in the REPL context' }
  );
}
export {
  defineReplBindings
};
//# sourceMappingURL=repl_bindings-TSQ2Y3GO.js.map